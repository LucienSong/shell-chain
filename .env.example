# Docker Compose environment variables for production deployment.
# Copy to .env and customize before deploying.

# Grafana admin password (REQUIRED — do not use default in production)
GF_ADMIN_PASSWORD=changeme
